{
  "name": "ai-clip-agent-mvp",
  "version": "0.1.0",
  "private": true,
  "description": "Local AI-first clip editor MVP powered by a Python backend.",
  "scripts": {
    "dev": "python3 server.py",
    "worker": "python3 worker.py",
    "worker:once": "python3 worker.py --once",
    "check": "python3 -m py_compile *.py && node --check static/app.js && node --check static/components.js && find static/js -name '*.js' -print0 | xargs -0 -n1 node --check"
  }
}
